# 单 Task 实现报告（IMPL_REPORT）

> 由 Implementer 在每次实现完成后产出。每次重试都在文件末尾追加新的 `## Attempt N` 章节，不要覆盖前次。

- **任务 ID**：{{T-XXX}}
- **任务名称**：{{...}}
- **本次尝试**：Attempt {{N}}
- **完成时间**：{{datetime}}

---

## Attempt {{N}}

### 1. 任务理解复述

- **任务目标**：{{用自己的话复述}}
- **产出文件**：{{清单}}
- **关键约束**：{{摘录与本任务最相关的 3~5 条约束}}
- **验收标准**：{{逐条复述}}
- **疑问**：{{如无疑问写"无"；如有疑问而仍按规格书实现，必须在 §4 偏差章节说明}}

### 2. 产出文件清单

| 文件 | 行数 | 说明 |
|---|---|---|
| `path/to/file_1.go` | {{N}} | {{...}} |
| `path/to/file_1_test.go` | {{N}} | {{...}} |

### 3. 验收标准自检

- [x] **验收标准 1**：{{摘要}} —— 已实现于 `{{file:line}}`
- [x] **验收标准 2**：{{摘要}} —— 已实现于 `{{file:line}}`
- [ ] **验收标准 3**：{{摘要}} —— 未满足，原因：{{...}}

### 4. 规格书符合性自检

#### 4.1 数据结构

- [x] 字段名称与规格书一致
- [x] 字段类型与规格书一致
- [x] tag 正确（json / gorm / db / validate）
- [x] 枚举值与规格书一致

#### 4.2 接口契约

- [x] 路径与规格书一致
- [x] 方法签名与规格书一致
- [x] 错误码与规格书一致
- [x] 幂等实现与规格书一致

#### 4.3 流程

- [x] Step 1 已实现于 `{{file:line}}`
- [x] Step 2 已实现于 `{{file:line}}`
- [x] Step N 已实现于 `{{file:line}}`
- [x] 异常分支：{{描述}} 已实现于 `{{file:line}}`

#### 4.4 编码约束

- [x] 金额使用 `int64`，单位"分"
- [x] DB 操作设置 3 秒超时，位置 `{{file:line}}`
- [x] RPC 调用设置 1 秒超时，位置 `{{file:line}}`
- [x] 错误使用 `%w` 包装
- [x] 日志结构化、含 trace_id
- [x] 所有 I/O 传递 ctx
- [x] 无硬编码配置值
- [x] 资源 `defer Close`

### 5. 测试自检

- [x] 测试文件：{{file}}
- [x] 正常路径用例：{{N}} 个
- [x] 异常路径用例：{{N}} 个（覆盖 {{...}}）
- [x] 边界条件用例：{{N}} 个
- [x] mock 使用：{{库名 + 在接口层 mock}}

### 6. 规格书偏差（如有）

> 如无偏差写"无"。任何偏离规格书的实现必须显式记录。

#### 偏差 1

- **偏差点**：规格书要求 {{X}}，实际实现为 {{Y}}
- **原因**：{{...}}
- **影响**：{{对其他模块的影响 / 无}}
- **建议**：☐ 修改规格书 ☐ 修改实现 ☐ 维持现状（已与用户沟通）

#### `// SPEC_QUESTION:` 注释清单

| 文件 | 行号 | 内容 |
|---|---|---|
| `{{file}}` | {{line}} | {{...}} |

### 7. 未完成项与原因

> 如全部完成写"无"。

- {{...}}

---

> Implementer 完成本报告后，触发 Verifier 进行校验，产出 `VERIFY_REPORT.md`。
